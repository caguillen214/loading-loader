describe('Angular and llLib Integration tests', function() {
  it('should track modules that are created', function() {
    // angular.module('testModule', []);
    // expect(llLib.getModule('testModule',true)).toBeTruthy();
  });
  it('should track dependencies of modules as modules loaded', function() {
    // angular.module('testModule', ['a','b']);
    // expect(llLib.getModule('a')).toBe('a');
    // expect(llLib.getModule('b')).toBe('b');
  });
})